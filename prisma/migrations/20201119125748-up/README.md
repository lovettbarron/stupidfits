# Migration `20201119125748-up`

This migration has been generated by Andrew Lovett-Barron at 11/19/2020, 1:57:48 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Notification" (
"id" SERIAL,
"seen" boolean   NOT NULL ,
"content" text   NOT NULL ,
"cta" text   NOT NULL ,
"created_at" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updated_at" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"userId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

ALTER TABLE "public"."Notification" ADD FOREIGN KEY ("userId")REFERENCES "public"."users"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201116223625-up..20201119125748-up
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Account {
   id                 Int       @id @default(autoincrement())
@@ -71,11 +71,12 @@
   Comment Comment[]
   Media   Media[]   @relation("UserMediaRelationship")
-  Review Review[]
+  Review     Review[]
+  Collection Collection[]
   @@map(name: "users")
-  Collection Collection[]
+  Notification Notification[]
 }
 model VerificationRequest {
   id         Int      @id @default(autoincrement())
@@ -251,8 +252,20 @@
   tags        Style[]
   Comment     Comment[]
 }
+model Notification {
+  id        Int      @id @default(autoincrement())
+  user      User     @relation(fields: [userId], references: [id])
+  seen      Boolean
+  content   String
+  cta       String
+  createdAt DateTime @default(now()) @map(name: "created_at")
+  updatedAt DateTime @default(now()) @map(name: "updated_at")
+
+  userId Int
+}
+
 enum ReviewFocus {
   ITEM
   STYLE
   FIT
```


